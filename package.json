{
  "name": "ts-rest-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "backend": "npx lerna run --scope backend",
    "nest": "npx lerna run --scope nest",
    "build": "npx lerna run build",
    "contract": "npx lerna run --scope contract",
    "frontend": "npx lerna run --scope frontend",
    "test": "npx lerna run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^8.1.2"
  },
  "dependencies": {},
  "workspaces": ["packages/*", "apps/*"]
}
